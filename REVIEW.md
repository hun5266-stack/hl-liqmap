# 검토 요청

이 레포를 처음 보는 사람에게. 무엇을 확인해야 하고 무엇은 이미 정해졌는지 적는다.

## 답하려는 질문

**청산가가 지지·저항으로 쓸 만한가.** 그리고 쓸 만하다면 전체 지갑 기준과 상위 지갑
기준 중 어느 쪽이 매매에 적합한가.

두 번째는 이미 답이 나왔다(아래 "정해진 것" 참고). 첫 번째가 미검증이고, 그래서
데이터를 모으는 중이다. **아직 검증을 시작하지 않았다** — 지금은 수집 단계다.

## 이미 정해진 것 (다시 따지지 말 것)

측정해서 결론 난 것들이다. 근거를 뒤집을 데이터가 있으면 그건 환영이지만,
근거 없이 되돌리자는 제안은 시간 낭비다.

| 결정 | 근거 |
|---|---|
| 상위 지갑 표본이 아니라 **전수 스캔** | 상위 10개는 현재가 근처 청산 포착률 **0%**, 상위 30개 33.6%, 상위 300개 86.5%. 리더보드가 accountValue 로 정렬돼 담보 두꺼운 계정만 뽑히고, 정작 터지는 얇은 계정이 빠진다 |
| **과거 소급 불가**, 지금부터 수집 | API 가 현재 상태만 준다. `liquidations` 타입은 422, S3 아카이브 접근 불가. 이미 일어난 청산 기록으로는 검증이 안 된다 — 가격이 간 자리에서 청산이 일어나므로 동어반복이다 |
| 워커 **36** | 러너 실측: 36 실패 0, 64 실패 19, 100 실패 74(25%). 빠진 계정은 지도의 구멍이고 소급이 안 된다 |
| **1시간 주기** | 4일 뒤 재조회 실측: 포지션 소멸 19.3%, 살아남은 것 중 청산가 2% 이상 이동 48.4%, 총량 -14.1%. 절반이 4일이면 못 쓴다 |
| **필터 없이 전량 저장** | 3주간 가격이 15~20% 움직이면 오늘 먼 청산가가 나중엔 가까워진다. 스냅샷당 120KB 라 아낄 이유가 없다 |
| 저장은 **UTC**, 표시만 KST | 이미 쌓인 97개와 어긋나면 안 된다 |

## 봐줬으면 하는 것

우선순위 순이다.

### 1. 수집이 온전한가 — `collect.py`

가장 치명적인 실패 모드는 **조용히 빠지는 계정**이다. 소급이 안 되므로 나중에 못 고친다.

- `scan()` 이 실패를 삼키지 않는지. `fetch()` 는 `post()` 가 3회 재시도 후 `None` 을
  반환하면 그냥 건너뛴다 — 실패 건수가 어디에도 안 남는다. **이게 맞는 설계인가?**
- 리더보드에서 빠진 계정은? `leaderboard()` 는 거래 이력이 있는 계정만 준다고 가정한다
- `holders.json` 갱신 가드(직전 대비 70% 미만이면 건너뜀)가 충분한가

### 2. 출처 분류가 맞는가 — `vps/report.py` 의 `origin()`

지금 이 칸의 물량을 셋으로 가른다: `held` / `moved_in` / `new`.
`moved_in` 이 핵심이다 — **새 돈이 아닌데 지도에서는 새로 생긴 것처럼 보인다.**
가격이 빠지면 롱은 담보가 줄어 청산가가 가격 쪽으로 올라오기 때문이다.

- 하루 전 스냅샷에 계정이 없으면 전부 `new` 로 친다. 그런데 그 계정이 **하루 전에는
  BTC 가 아닌 다른 코인만 들고 있었다면?** 지금 코드는 BTC 포지션만 저장하므로
  구분이 안 된다. 이게 얼마나 오염을 일으키는지 봐줬으면 한다
- `liq` 가 `None` 인 경우(청산가 없음, 전체의 약 19%)를 어떻게 다루는 게 맞나

### 3. 검증 설계가 통계적으로 성립하는가 — `README.md` 의 "검증 설계"

가설 셋을 데이터 보기 전에 적어뒀다. 지켜야 할 것도 적었다(대조군, 매물대와 분리).
그런데 아직 안 정한 게 있고, 정해야 할 시점이 검증 시작 **전**이다.

- **"가격이 클러스터에 닿았다"의 기준을 안 정했다.** ±0.3% 냐 ±1% 냐에 따라
  사건 수가 크게 달라진다. 어떻게 정하는 게 사후 편향을 피하나
- **사건 독립성.** 같은 가격대를 몇 시간 간격으로 반복해서 스치면 독립 사건이 아니다.
  지금 표본에서 서로 다른 가격대가 6~11개뿐이다. 어떻게 묶어야 하나
- 대조군을 "청산이 없는 구간" 으로 잡으면 그 구간은 애초에 가격이 잘 안 가는
  자리일 수 있다. 선택 편향을 어떻게 막나

### 4. 알려진 한계 — 이건 확인만

- **하이퍼리퀴드 한 거래소.** BTC 미결제약정이 바이낸스의 약 1/3 이고 바이낸스·바이빗은
  조회 자체가 불가능하다. 전체 시장 지도가 아니다
- **cross 마진 청산가는 계좌 전체 담보 기준.** 그 포지션이 아니라 계좌가 무너지는 값이다.
  최신 스냅샷 기준 약 78% 가 cross 다
- 스냅샷 하나 안에 최대 17분 시차가 섞여 있다. 캔들이 아니다

## 데이터 구조

`data/YYYY-MM/YYYYMMDDTHHMM_full.csv.gz` — gzip CSV.

```
#ts,<ISO8601 UTC>,px,<가격>,mode,full,scanned,<계정수>,btc_positions,<건수>,elapsed_s,<초>
addr,size,entry,liq,lev,margin,pnl,acct_value
0x…,-7.329440,78632.20,234635.63,3,cross,-64.36,1173198.39
```

`size` 는 부호 있는 BTC 수량(양수 롱, 음수 숏), `liq` 는 비면 청산가 없음.

읽는 예시는 `vps/report.py` 의 `load_snapshots()` 와 `load_full()` 을 보면 된다.

## 돌려보려면

수집기는 전수 스캔에 17분이 걸리고 하이퍼리퀴드 API 를 44,900번 때린다.
**검토용으로 돌릴 필요는 없다.** 이미 쌓인 데이터로 분석하면 된다.

```
python vps/report.py --dry --days 7 --out /tmp/x.png   # 전송 없이 그리기만
```

`--dry` 없이 돌리면 `/root/.discord_webhook` 을 읽어 전송을 시도한다.
그 파일이 없으면 조용히 건너뛴다.
