# 검토 후 합의한 개선 사항

2026-09-07 로컬 수정. GitHub 푸시·VPS 배포·실제 수집·Discord 전송은 수행하지 않았다.

| 합의 항목 | 반영 |
|---|---|
| 1. 개선 이후 자료 사용 | 리포트 기본 시작을 UTC 2026-09-03 13:00으로 설정. `--since`/`LIQMAP_SINCE`로 변경 가능. 초기 필터 적용·known 파일 제외 |
| 2. 조회 실패와 미보유 분리 | 모든 조회 대상의 결과·시각·시도 수·오류 이유를 압축 상태 파일에 저장. 요청별 3회 후 최종 실패 주소만 한 차례 더 조회(주소당 최대 6회) |
| 3. 전체 지갑 완전성 | 리더보드 밖 모든 지갑을 포착한다고 주장하지 않음. 기존 추적 지갑은 명단에서 빠져도 조회하고 실패 시 유지 |
| 4. 브리핑 분류 | 수량 순증·축소·롱↔숏 전환·청산가 이동을 분리. 이전 미보유 성공 기록이 있어야 신규로 분류. 과거 기록이나 실패는 미확인 |
| 5. 청산가 없음/cross | 원본 값 유지. 이동 원인·신규 자금 유입을 단정하지 않음 |
| 6. 가격과 표시 | Hyperliquid BTC 5분봉으로 통일. 별도 마크가격 수집기·저장·노란 선·도달 판정. 시작/완료/가격 시각 기록. $125 좌표 오차와 미래 지도 선행 표시 수정 |
| 7. GitHub 저장 실패 | VPS에서 최종 실패 시 Discord 브리핑 첫 줄에 경고. 서버 원본으로 브리핑 계속. 별도 재업로드 대기 보관소는 만들지 않음 |
| 8. 집중도 | 기존 집중도 표시 유지 |
| 9. 검증 설계 | 승률/백테스트 기준을 이번에 확정하거나 구현하지 않음 |
| 10. 재현 | 외부 통신을 차단한 테스트와 과거 데이터 미리보기 제공 |

## 파일과 운영 방식

기존 CSV 열은 유지한다. 새 파일명은 UTC 마이크로초까지 포함해 동시 실행의 이름 충돌 가능성을 낮춘다. `#ts`는 완료 시각, `px`는 완료 시 조회한 마크가격이며 실패 시 시작 가격과 그 시각·오류를 표시한다. CSV와 같은 이름의 `.status.json.gz`에는 주소별 상태, `.marks.json.gz`에는 마크가격 표본, `.candles.json.gz`에는 완료된 거래가격 5분봉을 저장한다. `data/`와 함께 GitHub로 보관된다.

미보유 성공 응답과 API 실패를 다르게 처리한다. 현재 BTC 포지션이 있으면 이전 미확인 상태에서도 현재 물량에는 포함하되 신규라고 단정하지 않는다. 현재 실패 주소의 이전 수량을 현재 지도에 넣지 않는다. 이전 수량이 확인되지 않은 경우를 브리핑에서 별도로 표시한다. 같은 방향의 두 수량에서 최솟값은 기존 수량, 양의 차이는 순증으로 계산한다. 이는 두 관측 시점의 비교이며 중간 거래 내역이나 입출금의 추정이 아니다.

마크가격은 지갑 스캔과 독립적으로 5초마다 공개 자산 컨텍스트를 조회하는 작은 서비스가 기록한다. 지갑별 45,000회 조회를 추가 실행하는 서비스가 아니다. 하루 약 17,280회의 별도 가격 요청이 추가된다. 기록은 레포 밖에 있어 매시간 Git 초기화와 충돌하지 않는다. 설치 전에는 시작·종료 시각의 드문 마크가격만 있고, 그 사이 도달 여부는 미확인으로 표시한다. 15초보다 큰 기록 공백은 선으로 잇지 않는다. 마크가격 표본이 구간 내부에 있거나 짧은 표본 간격에서 구간 양쪽을 통과한 경우를 구분하며 실제 강제 청산으로 단정하지 않는다.

현재 지도는 수집 완료부터 다음 수집까지 표시하고 최대 90분 후에는 공백으로 둔다. 90분은 표시의 오래됨을 나타내는 설정이며 검증용 최적 임계값으로 정한 것은 아니다. 과거 성공 상태 미기록/최종 실패 지도는 점무늬로 표시한다. 현재 기준가격을 중심으로 가격대별 물량을 표시하며 가격선의 최신값과 시각을 혼동하지 않는다.

VPS에서 이미지와 브리핑은 GitHub 저장과 별도로 서버 자료를 사용한다. 저장 실패 경고를 첫 줄에 넣고, Discord 본문 한도를 넘는 상세 브리핑은 `briefing.txt`로 함께 첨부한다. Discord 자체에 접속할 수 없으면 메시지를 보장할 수 없으며 서버 로그에 리포트 실패가 남는다. GitHub Actions 백업에는 Discord 전송 단계가 없으므로 그 실행의 저장 실패는 Actions 오류로 표시한다.

## 검증

```powershell
python -X utf8 -m unittest discover -s tests -v
python -X utf8 vps/report.py --offline --as-of 2026-09-05T08:17:12.919246+00:00 --out analysis/updated_map.png --text-out analysis/updated_briefing.txt
```

`--offline`은 API와 Discord를 사용하지 않는다. `--dry`는 Discord만 건너뛰며 가격 캔들 API는 조회한다. 기존 데이터에는 가격 캔들이 없으므로 오프라인 미리보기는 가격선을 생성하지 않는다.

운영 반영은 `vps/README.md`의 설치 절차가 별도로 필요하다. 마크가격 기록기 설치와 서버 실행본 교체를 하지 않은 상태에서는 로컬 수정만으로 서버 동작이 바뀌지 않는다.
